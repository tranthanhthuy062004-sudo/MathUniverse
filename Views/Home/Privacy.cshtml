@{
    // Page intentionally removed. Keeping file minimal to avoid 404 if routed.
    Layout = "_Layout";
    ViewData["Title"] = "Nội dung đã được chuyển";
}

<div class="container py-5 text-center">
    <div class="alert alert-info rounded-4 shadow-sm">
        <i class="fas fa-info-circle"></i> Trang này đã được chuyển. Vui lòng quay lại Trang chủ.
    </div>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary rounded-pill px-4">
        <i class="fas fa-home"></i> Về Trang chủ
    </a>
</div>

